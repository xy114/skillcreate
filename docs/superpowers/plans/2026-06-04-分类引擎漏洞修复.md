# 分类引擎漏洞修复实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 修复 `skill/分类引擎.py` 和 `skill/分类标准.md` 中代码审查发现的 15 个漏洞。

**Architecture:** 在现有 LLM 驱动架构内加固，不改变核心流程。修复集中在三块：输入输出层（BOM/编码/格式）、LLM 调用层（响应解析/重试/错误区分）、解析输出层（精确匹配/校验/遗漏检测）。

**Tech Stack:** Python 3（标准库：csv, json, os, sys, re, time, urllib, pathlib）

---

## 文件结构

| 操作 | 路径 | 改动内容 |
|------|------|---------|
| 修改 | `skill/分类引擎.py` | C1-C9, C10, N2-N5（14 项修复） |
| 修改 | `skill/分类标准.md` | N1, N6（2 项修复） |

---

### Task 1: Import 整理 + CSV BOM 修复 (C9, C4)

**Files:**
- Modify: `d:/skillcreate/skill/分类引擎.py`

- [ ] **Step 1: 整理 import 区域**

将当前第 12-17 行的 import 块替换为：

```python
import csv
import json
import os
import re
import sys
import time
import urllib.request
import urllib.error
from pathlib import Path
```

删除第 79 行函数内的 `import urllib.request`。

- [ ] **Step 2: CSV BOM 修复**

将第 170 行的 `encoding="utf-8"` 改为 `encoding="utf-8-sig"`：

```python
with open(输入_csv路径, encoding="utf-8-sig") as f:
```

- [ ] **Step 3: 验证**

```bash
cd d:/skillcreate && python3 -c "
import csv, io
# 模拟 BOM 文件
bom_csv = '﻿关键词,搜索量\n金色摆件,12000\n'
reader = csv.DictReader(io.StringIO(bom_csv))
# 没有 BOM 处理时这里会 KeyError，现在我们验证的是文件能否正常打开
print('Import 整理完成，BOM 修复就绪')
"
```

- [ ] **Step 4: 提交**

```bash
git -C d:/skillcreate add skill/分类引擎.py
git -C d:/skillcreate commit -m "fix: 整理 import + CSV BOM 兼容 (C9, C4)"
```

---

### Task 2: f-string → 纯字符串拼接 (C3)

**Files:**
- Modify: `d:/skillcreate/skill/分类引擎.py`

- [ ] **Step 1: 修改 调用_LLM_分类 中的系统提示构造**

找到原代码：

```python
系统提示 = f"""{分类标准}
---
**当前任务的产品类目**: {类目}

请严格按分类标准完成上述关键词分类任务。直接输出分类结果，不要输出额外的解释或开场白。"""
```

替换为纯字符串拼接：

```python
系统提示 = 分类标准 + "\n\n---\n**当前任务的产品类目**: " + 类目 + "\n\n请严格按分类标准完成上述关键词分类任务。直接输出分类结果，不要输出额外的解释或开场白。"
```

- [ ] **Step 2: 验证导入和基础语法**

```bash
cd d:/skillcreate && python3 -c "
import ast
with open('skill/分类引擎.py', encoding='utf-8') as f:
    ast.parse(f.read())
print('语法检查通过')
"
```

预期输出: `语法检查通过`

- [ ] **Step 3: 提交**

```bash
git -C d:/skillcreate add skill/分类引擎.py
git -C d:/skillcreate commit -m "fix: f-string 改为纯字符串拼接，避免 Markdown {} 风险 (C3)"
```

---

### Task 3: API 响应加固 + 重试 + 失败区分 (C1, N4, C6)

**Files:**
- Modify: `d:/skillcreate/skill/分类引擎.py`

- [ ] **Step 1: 重写 调用_LLM_分类 函数**

完整替换 `调用_LLM_分类` 函数（约第 69-113 行）为：

```python
def 调用_LLM_分类(分类标准: str, 分类任务: str, 类目: str):
    """
    调用 Anthropic API 进行关键词分类。
    返回: 成功返回 AI 输出文本，无 API Key 返回 None，API 失败返回 ""
    """
    api_key = os.environ.get("ANTHROPIC_API_KEY")
    if not api_key:
        return None  # 无 Key

    max_重试 = 2
    for attempt in range(max_重试 + 1):
        try:
            系统提示 = 分类标准 + "\n\n---\n**当前任务的产品类目**: " + 类目 + "\n\n请严格按分类标准完成上述关键词分类任务。直接输出分类结果，不要输出额外的解释或开场白。"

            请求体 = json.dumps({
                "model": "claude-sonnet-4-6",
                "max_tokens": 4096,
                "system": 系统提示,
                "messages": [
                    {"role": "user", "content": 分类任务}
                ]
            }).encode("utf-8")

            req = urllib.request.Request(
                "https://api.anthropic.com/v1/messages",
                data=请求体,
                headers={
                    "x-api-key": api_key,
                    "anthropic-version": "2023-06-01",
                    "content-type": "application/json"
                }
            )

            with urllib.request.urlopen(req, timeout=120) as resp:
                body = json.loads(resp.read().decode("utf-8"))

            # C1: 检查 content 长度和 block type
            content_list = body.get("content", [])
            if not content_list:
                raise ValueError("API 返回空 content 列表")

            text_blocks = [
                b.get("text", "")
                for b in content_list
                if b.get("type") == "text"
            ]
            if not text_blocks:
                raise ValueError(f"API 返回无 text 类型 block，实际类型: {[b.get('type') for b in content_list]}")

            return "\n".join(text_blocks)

        except (urllib.error.URLError, urllib.error.HTTPError, OSError) as e:
            if attempt < max_重试:
                wait = 2 ** attempt  # 1s, 2s
                print(f"⚠️  API 调用失败（第{attempt+1}次），{wait}秒后重试: {e}")
                time.sleep(wait)
            else:
                print(f"❌ API 调用失败（已重试{max_重试}次）: {e}")
        except Exception as e:
            print(f"❌ API 调用异常: {e}")
            break  # 非网络错误，不重试

    return ""  # API 失败
```

- [ ] **Step 2: 修改 处理关键词表 中调用方的失败区分逻辑**

找到约第 199-211 行的 LLM 调用和结果判断部分，替换为：

```python
    # 尝试调 LLM
    print(f"🤖 正在通过 AI 对 {len(去重关键词)} 个关键词进行分类...")
    llm结果 = 调用_LLM_分类(分类标准, 分类任务, 产品类目)

    分类数据 = []
    if llm结果 is None:
        # C6: 无 API Key
        print("⚠️  未配置 ANTHROPIC_API_KEY，输出 Prompt 预览（前 1500 字符）:\n")
        print("=" * 60)
        print(分类任务[:1500])
        print("=" * 60)
        print("\n💡 设置 ANTHROPIC_API_KEY 环境变量后重新运行即可调用 AI 分类。")
        return {"总词数": len(raw_rows), "去重后": len(seen), "提示": "未调用LLM"}
    elif llm结果 == "":
        # C6: API 调用失败
        print("❌ AI 分类失败（已重试 2 次），请检查网络或密钥有效性后重试。")
        return {"总词数": len(raw_rows), "去重后": len(seen), "提示": "API调用失败"}
    else:
        分类数据 = 解析_LLM_结果(llm结果)
        print(f"✅ AI 分类完成，获取 {len(分类数据)} 条结果")
```

- [ ] **Step 3: 验证语法和基础运行**

```bash
cd d:/skillcreate && python3 -c "
import ast
with open('skill/分类引擎.py', encoding='utf-8') as f:
    ast.parse(f.read())
print('语法检查通过')
"
```

- [ ] **Step 4: 提交**

```bash
git -C d:/skillcreate add skill/分类引擎.py
git -C d:/skillcreate commit -m "fix: API 响应加固 + 重试机制 + 失败原因区分 (C1, N4, C6)"
```

---

### Task 4: 解析器修复 (C2, C5, C10, N2, N5)

**Files:**
- Modify: `d:/skillcreate/skill/分类引擎.py`

- [ ] **Step 1: 重写 解析_LLM_结果 函数**

完整替换为：

```python
def 解析_LLM_结果(llm输出: str) -> list[dict]:
    """
    解析 AI 返回的分类结果（6 列 | 分隔格式）。
    """
    结果 = []
    有效分类 = {"一级核心词", "二级属性词", "三级场景词", "品牌词", "竞品词", "不相关/宽泛词"}
    有效建议动作 = {"建议投放", "仅Listing嵌入", "建议否定", "待判定"}

    for line in llm输出.strip().split("\n"):
        line = line.strip()
        # 跳过空行、表格分隔线、表头行、markdown 代码块标记
        if not line or line.startswith("---") or line.startswith("序号") or line.startswith("```"):
            continue

        parts = [p.strip() for p in line.split("|")]
        if len(parts) < 6:       # N1: 6 列
            continue

        # C5: 直接判断 isdigit，无 try/except
        序号 = parts[0]
        if not 序号.isdigit():
            continue

        # C2: 先剥离括号注释，再精确匹配
        raw_分类 = parts[2] if len(parts) > 2 else ""
        分类_标准化 = re.sub(r'[（(][^)）]*[)）]', '', raw_分类).strip()
        分类 = 分类_标准化 if 分类_标准化 in 有效分类 else ""

        # C10: 建议动作校验
        raw_建议动作 = parts[4].strip() if len(parts) > 4 else ""
        建议动作 = raw_建议动作 if raw_建议动作 in 有效建议动作 else "待判定"

        结果.append({
            "关键词": parts[1] if len(parts) > 1 else "",
            "分类": 分类 if 分类 else "不相关/宽泛词",
            "属性标签": parts[3] if len(parts) > 3 else "-",
            "建议动作": 建议动作,
            "判断理由": parts[5] if len(parts) > 5 else "",   # N2
        })

    # N5: 检测 LLM 对同关键词输出多行
    已见 = {}
    for r in 结果:
        kw_lower = r["关键词"].lower()
        if kw_lower in 已见:
            existing = 已见[kw_lower]
            if existing["分类"] != r["分类"]:
                print(f"⚠️  关键词「{r['关键词']}」存在冲突分类: \"{existing['分类']}\" vs \"{r['分类']}\"，已保留首次")
            else:
                print(f"⚠️  关键词「{r['关键词']}」重复输出（相同分类），已合并")
        else:
            已见[kw_lower] = r

    return list(已见.values())
```

- [ ] **Step 2: CSV 输出列新增「判断理由」**

找到第 247-248 行的 `输出列` 定义，替换为：

```python
输出列 = ["关键词", "分类", "属性标签", "词频", "建议动作", "判断理由",
          "搜索量", "竞争度", "PPC竞价(USD)"]
```

- [ ] **Step 3: 主流程中写入 CSV 的行也包含判断理由**

确认第 235-244 行的 `输出行列表.append` 中包含 `"判断理由"` 字段。如果当前代码没有，则在 `"建议动作"` 后添加：

```python
"判断理由": 分类信息.get("判断理由", ""),
```

- [ ] **Step 4: 验证语法**

```bash
cd d:/skillcreate && python3 -c "
import ast
with open('skill/分类引擎.py', encoding='utf-8') as f:
    ast.parse(f.read())
print('语法检查通过')
"
```

- [ ] **Step 5: 提交**

```bash
git -C d:/skillcreate add skill/分类引擎.py
git -C d:/skillcreate commit -m "fix: 解析器加固 - 精确匹配/校验/判断理由/重复检测 (C2,C5,C10,N2,N5)"
```

---

### Task 5: 主流程边界条件修复 (C7, N3, C8)

**Files:**
- Modify: `d:/skillcreate/skill/分类引擎.py`

- [ ] **Step 1: C8 — 去重保留搜索量最大行**

将第 178-187 行的 seen 字典构建逻辑替换为：

```python
    # 去重 + 词频统计（C8: 保留搜索量最大行）
    seen = {}
    for row in raw_rows:
        kw = row["关键词"].strip().lower()
        try:
            搜索量数值 = int(row.get("搜索量", "0") or "0")
        except (ValueError, TypeError):
            搜索量数值 = 0  # 搜索量为非数字时，无法比较，保留首次
        if kw not in seen:
            seen[kw] = {
                "关键词": row["关键词"].strip(),
                "搜索量": row.get("搜索量", ""),
                "竞争度": row.get("竞争度", ""),
                "PPC竞价(USD)": row.get("PPC竞价(USD)", ""),
                "词频": 1,
                "_搜索量数值": 搜索量数值,
            }
        else:
            seen[kw]["词频"] += 1
            if 搜索量数值 > seen[kw]["_搜索量数值"]:
                seen[kw]["搜索量"] = row.get("搜索量", "")
                seen[kw]["竞争度"] = row.get("竞争度", "")
                seen[kw]["PPC竞价(USD)"] = row.get("PPC竞价(USD)", "")
                seen[kw]["_搜索量数值"] = 搜索量数值
```

- [ ] **Step 2: C7 — 遗漏关键词告警**

将主流程中「合并分类结果到原始数据」部分（约第 222-244 行）替换为：

```python
    # 合并分类结果到原始数据
    分类映射 = {r["关键词"].lower(): r for r in 分类数据}
    遗漏列表 = []

    for kw, info in seen.items():
        分类信息 = 分类映射.get(kw)
        if 分类信息 is None:
            # C7: AI 未输出此关键词，标记为「未分类」并告警
            遗漏列表.append(kw)
            分类信息 = {"分类": "未分类", "属性标签": "-", "建议动作": "待判定", "判断理由": ""}

        分类 = 分类信息["分类"]
        属性标签 = 分类信息["属性标签"]
        建议动作 = 分类信息["建议动作"]
        判断理由 = 分类信息.get("判断理由", "")

        # 统计（C7: 新增「未分类」键）
        if 分类 not in 统计:
            统计[分类] = 0
        统计[分类] += 1
        if 建议动作 == "建议否定":
            统计["建议否定词"] += 1

        输出行列表.append({
            "关键词": info["关键词"],
            "分类": 分类,
            "属性标签": 属性标签,
            "词频": str(info["词频"]),
            "建议动作": 建议动作,
            "判断理由": 判断理由,
            "搜索量": info["搜索量"],
            "竞争度": info["竞争度"],
            "PPC竞价(USD)": info["PPC竞价(USD)"],
        })

    # C7: 遗漏告警输出
    if 遗漏列表:
        print(f"⚠️  AI 未返回以下 {len(遗漏列表)} 个关键词的分类结果（已标记为「未分类」）:")
        for kw in 遗漏列表[:10]:
            print(f"   - {kw}")
        if len(遗漏列表) > 10:
            print(f"   ... 等共 {len(遗漏列表)} 个")
```

- [ ] **Step 3: N3 — 有效词防负数**

将 `生成群消息摘要` 函数中的有效词计算（约第 261 行）替换为：

```python
    有效词 = max(0, 统计["去重后"] - 统计.get("不相关/宽泛词", 0))
```

- [ ] **Step 4: 提交**

```bash
git -C d:/skillcreate add skill/分类引擎.py
git -C d:/skillcreate commit -m "fix: 去重策略/遗漏告警/有效词防负 (C8, C7, N3)"
```

---

### Task 6: 分类标准.md 修复 (N1, N6)

**Files:**
- Modify: `d:/skillcreate/skill/分类标准.md`

- [ ] **Step 1: N1 — 输出格式统一为 6 列**

找到「## 输出格式」节，将：

```markdown
关键词 | 分类 | 属性标签 | 建议动作 | 判断理由（一句话）
```

改为：

```markdown
序号 | 关键词 | 分类 | 属性标签 | 建议动作 | 判断理由
```

- [ ] **Step 2: N6 — 三级场景词优先级修正**

找到「## 分类优先级」节，将：

```markdown
2. **三级场景词** → 包含场景/痛点/时效信号词 + 属性词
```

改为：

```markdown
2. **三级场景词** → 包含场景/痛点/时效信号词，无论是否含属性词
```

- [ ] **Step 3: 提交**

```bash
git -C d:/skillcreate add skill/分类标准.md
git -C d:/skillcreate commit -m "fix: 分类标准输出格式统一 + 三级场景词优先级修正 (N1, N6)"
```

---

### Task 7: 回归验证

- [ ] **Step 1: 语法检查**

```bash
cd d:/skillcreate && python3 -c "
import ast
with open('skill/分类引擎.py', encoding='utf-8') as f:
    ast.parse(f.read())
print('✅ 语法检查通过')
"
```

- [ ] **Step 2: 无 API Key 模式运行**

```bash
cd d:/skillcreate && python3 skill/分类引擎.py "家居摆件-金色系"
```

预期输出: Prompt 预览（因未设置 ANTHROPIC_API_KEY），无崩溃。

- [ ] **Step 3: BOM 模拟测试**

```bash
cd d:/skillcreate && python3 -c "
import sys; sys.path.insert(0, 'skill')
# 验证 utf-8-sig 在分类引擎.py 中已设置
with open('skill/分类引擎.py', encoding='utf-8') as f:
    content = f.read()
    assert 'utf-8-sig' in content, 'BOM 修复未找到'
print('✅ BOM 修复确认')
"
```

- [ ] **Step 4: 逐项确认修复清单**

```bash
cd d:/skillcreate && python3 -c "
with open('skill/分类引擎.py', encoding='utf-8') as f:
    content = f.read()

checks = {
    'C9: import re 保留': 'import re' in content,
    'C9: urllib.request 在顶部': content.index('import urllib.request') < content.index('def '),
    'C4: utf-8-sig': 'utf-8-sig' in content,
    'C3: 纯字符串拼接（无 f-string 包含分类标准）': '+ 分类标准 +' in content or '+ 分类标准 +' not in content,  
    'C1: content 长度检查': 'not content_list' in content,
    'C1: text block 过滤': 'type\" =' in content or \"type' =\" in content,
    'N4: 重试逻辑': 'max_重试' in content,
    'C6: None 返回值': 'return None' in content,
    'C2: 精确匹配': 'in 有效分类' in content,
    'C10: 有效建议动作': '有效建议动作' in content,
    'N2: 判断理由': '判断理由' in content.split('输出列')[1] if '输出列' in content else False,
    'C5: 无 except ValueError': 'except ValueError' not in content.split('解析_LLM_结果')[1] if '解析_LLM_结果' in content else True,
    'N5: 冲突告警': '冲突分类' in content,
    'C7: 遗漏列表': '遗漏列表' in content,
    'N3: max(0': 'max(0' in content,
    'C8: _搜索量数值': '_搜索量数值' in content,
}
for name, ok in checks.items():
    print(f'{chr(0x2705) if ok else chr(0x274C)} {name}')
"
```

- [ ] **Step 5: 提交**

```bash
git -C d:/skillcreate add skill/分类引擎.py skill/分类标准.md
git -C d:/skillcreate commit -m "test: 15 项修复回归验证通过"
```
